apply plugin: "scala"
apply plugin: "maven-publish"
apply plugin: "com.fipsoft.maven-repo-settings"

jar {
    baseName = "rx-scala-wrapper"
    version = "NEXT-SNAPSHOT"
}

task sourceJar(type: Jar) {
    from sourceSets.main.allScala
}

repositories {
    jcenter()
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId "eu.exante"
            artifactId "rx-scala-wrapper"
            version "0.1.2"

            from components.java
        }
    }

    repositories {
        mavenInternal {
            repo {
                id "thirdparty"
                url "http://nexus.ghcg.com/repository/thirdparty"
            }
        }
    }
}

dependencies {
    compile group: "org.scala-lang",       name: "scala-library",    version: "2.12.4"
    compile group: "io.reactivex.rxjava2", name: "rxjava",           version: "2.+"
    compile group: "org.reactivestreams",  name: "reactive-streams", version: "1.0.+"
}

buildscript {
    repositories {
        maven {
            url "https://plugins.gradle.org/m2/"
        }
    }

    dependencies {
        classpath "com.fipsoft.gradle:maven-repo-settings:0.3.0"
    }
}

